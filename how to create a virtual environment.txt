-----------------------------------------------------------------------------------


DO THIS!!!!:
python3 -m venv venv 
if the venv is missing a few things in scripts (like activate or bin or something)
do
deactivate
then python3 -m venv venv again

NOTE: if you create the venv and your python file is still not connected to it, go into scripts then drag Activate.ps1 to the terminal then press enter, your next python thing should have (venv) in green next to it. then you can install ur packages and itll install into venv

OLD SOLUTION:
python -m venv "C:\Users\laura\OneDrive\Desktop\Udemy Yu Shit\how to create virtual environment\venv"    
& "venv\Scripts\activate" - unnessessary, vscode should automatically activate, idk if this even works.




------------------------------------------------------------------

ENVIRONMENT VARIABLES
import os
from dotenv import load_dotenv

create .env file in folder with venv (NOT IN VENV FOLDER)
put the environment variable in .env like this:
MY_EMAIL="email@gmail.com"
PASSWORD="thisismypassword123"

in main.py:
import os
from dotenv import load_dotenv
MY_EMAIL = os.getenv("MY_EMAIL")
PASSWORD = os.getenv("PASSWORD")

REFER TO PYTHON DAY 47
